query Game($id: ID!) {
  game(id: $id) {
    id
    namespace,
    numTeams
  }
}

mutation CreateGame($numTeams: Int!) {
  createGame(numTeams: $numTeams) {
    id,
    namespace,
    numTeams
  }
}
